# TerrifiedBug Homebrew Tap

Casks for TerrifiedBug's macOS apps.

```bash
brew install --cask terrifiedbug/tap/tickerbar   # menu-bar stock ticker
```

Builds are signed with a Developer ID certificate and notarized by Apple, so
they open without a Gatekeeper prompt. TickerBar also updates itself through
Sparkle.
